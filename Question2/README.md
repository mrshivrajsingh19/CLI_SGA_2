# Question 2 – Process Management using fork()

This question demonstrates process management using the fork() system call in C.

## Files
- `process_monitor.c` – C source code for process management.
- `process_monitor` – Compiled executable.
- `screenshots/` – Screenshots showing the commands and execution results.

## Execution
The program was compiled and executed successfully. The screenshots directory contains the required execution screenshots.
